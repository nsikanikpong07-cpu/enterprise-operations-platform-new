# Application
NODE_ENV=development
PORT=3000

# PostgreSQL connection (used by backend TypeORM and the legacy pg Pool)
PGHOST=localhost
PGPORT=5432
PGDATABASE=enterprise_operations
PGUSER=enterprise_app
PGPASSWORD="REPLACE_WITH_YOUR_LOCAL_PASSWORD"

# Optional: overrides the individual PG* vars if set
DATABASE_URL=
